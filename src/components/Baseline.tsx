// Baseline rules
// Baseline should be semi-generative (until we settle on one that's cool)
// At every render, a baseline contained in a jsnon file should be displayed (?)

export function Baseline() {
  return <div>Baseline</div>;
}
