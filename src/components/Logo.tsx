// Logo should be semi-generative 
// It should use a collection of svg composed of the following letters: 
    // "b, "d", "e", "w"
    // there should be a total of 5 variation for each char so 20 svg in total
    // the svg paths should be in a json file
    // a function should randomly select a path for each char
    // the function should be called at each render

export function Logo() {
  return <div>Logo</div>;
}
